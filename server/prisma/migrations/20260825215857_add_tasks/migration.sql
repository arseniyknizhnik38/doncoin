-- Задания: храним только список полученных.
-- Прогресс считается на лету из уже существующих полей, поэтому отдельных
-- счётчиков и таблицы прогресса не нужно.

ALTER TABLE "User" ADD COLUMN "claimedTasks" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[];
